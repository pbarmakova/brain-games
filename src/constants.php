<?php

namespace BrainGames;

if (!defined('MIN_OPERAND')) {
    define('MIN_OPERAND', 0);
    define('MAX_OPERAND', 100);
    define('MIN_PROGRESSION_LENGTH', 5);
    define('MAX_PROGRESSION_LENGTH', 10);
    define('OPERATORS', ['+', '-', '*']);
    define('MIN_STEP', 1);
    define('MAX_STEP', 20);
}
